# 图片压缩工具

一个纯浏览器端运行的图片压缩工具。图片只在本地处理，不会上传到服务器。

## 功能

- 支持 JPG、PNG、WebP
- 默认压缩到 200KB 以下
- 自动寻找尽量高的压缩质量
- 必要时按比例降低分辨率以满足目标大小
- 支持原图和压缩图预览
- 支持下载压缩后的图片

## 使用

直接用浏览器打开 `index.html`，选择或拖入图片即可。

也可以部署到 GitHub Pages，入口文件就是 `index.html`。
